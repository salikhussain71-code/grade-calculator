# Smart Grade Calculator

My first Python project using `if/elif/else` and functions from CS50P.

## What it does
Takes a student score as input and returns a grade A-F with feedback.

## How to run
```bash
python grade_calculator.py
